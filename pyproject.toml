[project]
name = "chack-iconography"
version = "0.1.0"
description = "Cambridge Hackspace isometric network iconography pack generator"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
